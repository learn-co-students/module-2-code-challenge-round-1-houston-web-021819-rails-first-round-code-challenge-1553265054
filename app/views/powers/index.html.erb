<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
    name:  <%= link_to power.name, power_path(power) %>
      <br>
      Description: <%= link_to power.description, power_path(power) %>
    </li>
    <br>
  <% end %>
</ul>
