<h1>Create Heroine</h1>
<%= form_for @heroine do |h| %>
	Heroine Name: <%= h.text_field :name %></br>
	Super Name: <%= h.text_field :super_name %></br>
	<%= h.collection_select :power_id, Power.all, :id, :name %>
	<%= h.submit "Create Heroine" %>
<% end %>